get:
  summary: get channel
  tags:
    - channel
  responses:
    "200":
      description: A JSON array of channel names
      content:
        application/json:
          schema:
            type: array
            items:
              type: string
