# paths 파일 안의 각 api명세파일들 --> _index.yaml --> openapi.yaml --> build.yaml 순으로 합쳐지고 실행되는 구조.
# _index.yaml은 Router 혹은 resolver와 비슷한 역할

/user: # API endpoint
  $ref: "./user/user.get.yaml" # api 명세 파일

/channel/get:
  $ref: "./channel/channel.get.yaml"
/channel/create:
  $ref: "./channel/channel.create.yaml"
